# Configuration file for EventSetupTest_t

process TEST = {

	path p = { prod }

	es_source = PoolDBESSource { VPSet toGet = {
                                   {string record = "CSCPedestalsRcd"
                                     string tag = "mycsctest"
                                    } }
		    		    bool loadAll = true
                                    string connect = "sqlite_file:csctest.db"
			            string timetype = "runnumber" 
                                    untracked string catalog = "file:mycsccatalog.xml"
	                            untracked uint32 authenticationMethod = 1
				   }

	source = EmptySource {untracked int32 maxEvents = 9 untracked uint32 firstRun = 1 untracked uint32 numberEventsInRun = 3}

	module prod = CSCPedestalReadAnalyzer{}

        module print = AsciiOutputModule { }
        endpath ep = { print }
} 